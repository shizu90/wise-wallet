<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.0</version>
    </parent>

    <groupId>dev.gabriel.wisewallet</groupId>
    <artifactId>wise-wallet</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>

    <modules>
        <module>src/core</module>
        <module>src/user</module>
        <module>src/wallet</module>
        <module>src/bill</module>
        <module>src/budget</module>
        <module>src/recurringbill</module>
        <module>src/reminder</module>
        <module>src/category</module>
        <module>src/currency</module>
        <module>src/service-discovery</module>
        <module>src/api-gateway</module>
        <module>src/auth</module>
        <module>src/config</module>
        <module>src/token</module>
    </modules>

    <properties>
        <project.version>1.0.0</project.version>
        <project.encoding>UTF-8</project.encoding>
        <java.version>21</java.version>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>
        <maven.compiler.plugin.version>3.12.1</maven.compiler.plugin.version>
        <project.build.sourceEncoding>${project.encoding}</project.build.sourceEncoding>
        <spring.version>3.2.2</spring.version>
        <spring.doc.version>2.1.0</spring.doc.version>
        <spring.mongodb.version>4.2.0</spring.mongodb.version>
        <spring.kafka.version>3.1.1</spring.kafka.version>
        <spring.cloud.version>2023.0.0</spring.cloud.version>
        <spring.cloud.gateway.version>4.1.0</spring.cloud.gateway.version>
        <spring.cloud.eureka.server.version>4.1.0</spring.cloud.eureka.server.version>
        <spring.cloud.eureka.client.version>4.1.0</spring.cloud.eureka.client.version>
        <lombok.version>1.18.30</lombok.version>
        <junit.jupiter.api.version>5.10.0</junit.jupiter.api.version>
        <junit.jupiter.engine.version>5.10.0</junit.jupiter.engine.version>
        <mockito.version>5.3.1</mockito.version>
        <assertj.version>3.24.2</assertj.version>
        <guava.version>33.0.0-jre</guava.version>
        <postgresql.driver.version>42.6.0</postgresql.driver.version>
        <mongodb.driver.version>4.11.0</mongodb.driver.version>
        <javax.persistence.version>2.2</javax.persistence.version>
        <javax.validation.version>2.0.1.Final</javax.validation.version>
        <javax.servlet.version>4.0.1</javax.servlet.version>
        <jaxb.api.version>2.3.1</jaxb.api.version>
        <javax.activation.version>1.1.1</javax.activation.version>
        <jaxb.runtime.version>4.0.4</jaxb.runtime.version>
        <jackson.datatype.version>2.16.1</jackson.datatype.version>
        <jwt.version>0.11.5</jwt.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-jsr310</artifactId>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <version>${junit.jupiter.api.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <version>${junit.jupiter.engine.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <version>${mockito.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-junit-jupiter</artifactId>
            <version>${mockito.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>${assertj.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring.cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>
</project>